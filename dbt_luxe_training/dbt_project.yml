## Project
name: luxe_training
version: "1.0"
config-version: 2
profile: luxe_training

## Sources
model-paths: [models]
analysis-paths: [analysis]
test-paths: [tests]
seed-paths: [seeds]
macro-paths: [macros]
snapshot-paths: [snapshots]

## Target
target-path: target
clean-targets: [target, dbt_modules]

## Models
models:
  luxe_training:
    staging:
      +materialized: view
    marts:
      +materialized: table