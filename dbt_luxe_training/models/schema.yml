version: 2
models:
  - name: fact_sales
    columns:
      - name: sale_id
        tests: [not_null, unique]
      - name: quantity
        tests: [not_null]
  - name: dim_product
    columns:
      - name: product_id
        tests: [not_null, unique]
  - name: dim_store
    columns:
      - name: store_id
        tests: [not_null, unique]
